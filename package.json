{
  "name": "wangs-agent",
  "version": "0.1.2",
  "description": "Standalone interactive terminal chat CLI for Wangs Foundation projects, built on the Claude Agent SDK. Behaves like a general-purpose coding agent for everyday work; routes end-to-end feature builds through the agentic-feature-loop pipeline (real command gates, never model self-judgment) instead of freeform agent judgment.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "wangs-agent": "./dist/cli.mjs"
  },
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "import": "./dist/index.mjs"
    }
  },
  "scripts": {
    "build": "tsdown",
    "type-check": "tsc --noEmit",
    "lint": "oxlint src",
    "dev": "tsx src/cli.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.274",
    "agentic-feature-loop": "^0.1.4",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "oxlint": "^1.75.0",
    "tsdown": "^0.22.14",
    "tsx": "^4.23.13",
    "typescript": "~6.0.3"
  },
  "engines": {
    "node": ">=20"
  }
}
